<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Home</title>
</head>
<body>
    <h1>olá  </h1>
    <div class="variaveis">
        <%= nome %><br>
        <%= lang %><br>
        <%= isncritos %><br>
    </div>
    <div class="erro">
            <% if(flag){ %>
            <h3>Mensagen de erro</h3>
            <% }else{ %>
                <h3>Sem erros</h3>
            <% } %>
    </div>
    
    <div class="repeticao">   
        <% produtos.forEach(function(produto){%>
            <%= produto.nome %> ------------------ <%= produto.preco %><br>
        <% }); %>
    </div>
    
    <img src="/img/load.gif" alt="" srcset="">

</body>

</html>